## Packages
framer-motion | Smooth page transitions and micro-interactions

## Notes
- Tailwind Config assumption: using default tailwind installation with standard shadcn paths.
- Fonts: Using 'Syne' for display and 'DM Sans' for body text via Google Fonts.
- Mock Auth: Uses a React Context to select the current user from the DB list since true auth is not implemented.
